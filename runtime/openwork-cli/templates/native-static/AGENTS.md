# Native Static Project

This project was created from the Open Work Native Static template.

Use plain HTML, CSS, and JavaScript. The development server is Python `http.server` on port `<%= port %>`.
