# PI 维护指南

当你修改 `pi/` 内的运行时代码时，请使用这份指南。

## 规则

- 保持运行时的通用性。
- 不要把产品专属工作流加入默认运行时。
- 优先通过工具层或技能层扩展，而不是硬编码行为。
- 修改完成后运行 `cd pi && npm run check`。

## 运行时职责

- 会话管理
- 工作区处理
- 工具暴露
- 制品 API
- Web 搜索工具
- `discussion` 运行时
